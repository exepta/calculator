pub fn calculate(way: &str) -> &str {
    let mut result :&str = "";
    if !way.is_empty() {

    }
    return result;
}

/** Enum for handling math operators like + - * / */
enum Operators {
    ADDITION,
    SUBTRACTION,
    MULTIPLICATION,
    DIVIDED
}